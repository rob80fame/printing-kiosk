API CONFIGURATION

//install dependencies
    node-v24.18.0
    Redis-5.0.14.1
    postgresql-18.4-2 (keep your postgres password)
    clone evolution-api-2.3.7

//in evolution api install libraries
    npm install

//create .env file as .env.example in evolution-api folder editing password and api key

//create db
    createdb -U postgres evolution

//initialize db
    npx prisma migrate deploy --schema ./prisma/postgresql-schema.prisma

//start api
    npm start

//go to http://localhost:8080/manager/
//link your whatsapp
//wait user input

BACKEND and CONFIGURATION

//Install dependencies
    Python 3.12
    Office 2019 suite (for conversion from docx to pdf)
    SumatraPDF 3.6.1 (to print pdfs)

    pip install flask psycopg2-binary cryptography requests docx2pdf sv-ttk
    "C:\Program Files\PostgreSQL\18\bin\createdb.exe" -U postgres images

//Rename config_example.json to config.json and fill in with your data

//run the following in an administrator cmd

schtasks /create /tn "kiosk_cleanup" /tr "python 'C:\Users\rober\Downloads\printing-kiosk\printing-kiosk\cleanup.py'" /sc daily /st 20:00