API CONFIGURATION

//install dependencies
    node-v24.18.0
    Redis-5.0.14.1
    postgresql-18.4-2 (keep your postgres password)
    clone evolution-api-2.3.7

//install libraries
    npm install

//create .env file as .env.example in evolution-api folder and edit password and api key

//create db
    createdb -U postgres evolution

//initialize db
    npx prisma migrate deploy --schema ./prisma/postgresql-schema.prisma
    "C:\Program Files\PostgreSQL\18\bin\createdb.exe" -U postgres evolution

//start api
    npm start

//CHECK THE FOLLOWING
//go to http://localhost:8080/manager/
//link your whatsapp
//events>webhook>Enabled
//URL > http://127.0.0.1:8080/webhook
//Webhook Base64 > true
//Events> MESSAGES_UPSERT and SEND_MESSAGE > true
//SAVE

//CONFIGURATIONS
//Reject Calls > true
//Ignore Groups > true
//Always Online > true
//Read Messages > true
//Sync full history > false
//Read status > false

BACKEND and CONFIGURATION

//Install dependencies
    Python 3.12
    Office 2019 suite (for conversion from docx to pdf)
    SumatraPDF 3.6.1 (to print pdfs)

    pip install flask psycopg2-binary cryptography requests docx2pdf sv-ttk
    "C:\Program Files\PostgreSQL\18\bin\createdb.exe" -U postgres images

//Rename ex_config.py to config.py and fill in with your data